      - name: Запуск сборки на сервере
        run: |
          ./crave run --no-patch -v -- "mkdir -p pixel && cd pixel && repo init -u https://github.com/PixelOS-AOSP/manifest.git -b sixteen && rm -rf .repo/local_manifests && mkdir -p .repo/local_manifests && curl -sL https://raw.githubusercontent.com/${{ github.repository }}/main/mondrian.xml -o .repo/local_manifests/mondrian.xml && repo sync -c -j\$(nproc --all) --force-sync --no-clone-bundle --no-tags && source build/envsetup.sh && lunch aosp_mondrian-userdebug && mka bacon"
