name: PixelOS Mondrian Build

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 0 * * 0'  # Еженедельно в воскресенье (раскомментируйте если нужно)

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y curl git python3 python3-pip python3-venv wget

      - name: Install repo
        run: |
          mkdir -p $HOME/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > $HOME/bin/repo
          chmod +x $HOME/bin/repo
          echo "$HOME/bin" >> $GITHUB_PATH

      - name: Install crave via pip
        run: |
          # Устанавливаем crave через pip
          echo "📦 Installing crave via pip..."
          pip3 install crave --break-system-packages || pip3 install crave
          
          # Добавляем локальный bin в PATH
          export PATH="$HOME/.local/bin:$PATH"
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          
          # Проверяем установку
          echo "🔍 Checking installation..."
          if command -v crave &> /dev/null; then
            echo "✅ Crave installed successfully at: $(which crave)"
            echo "✅ Crave version:"
            crave version
          else
            echo "❌ Crave not found in PATH"
            echo "🔍 Searching for crave..."
            find /home -name crave -type f 2>/dev/null || true
            exit 1
          fi

      - name: Configure Crave
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          mkdir -p $HOME/.crave
          
          # Создаем конфиг
          cat << EOF > $HOME/.crave/crave.conf
          [crave]
          username = ${{ secrets.CRAVE_USERNAME }}
          token = ${{ secrets.CRAVE_TOKEN }}
          EOF
          
          echo "✅ Crave config created"
          
          # Проверяем аутентификацию
          echo "🔍 Testing authentication..."
          if crave clone list > /dev/null 2>&1; then
            echo "✅ Authentication successful!"
          else
            echo "❌ Authentication failed"
            echo "Please check your CRAVE_USERNAME and CRAVE_TOKEN secrets"
            exit 1
          fi

      - name: Setup project and sync
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          
          # Уникальное имя клона с датой
          CLONE_NAME="pixelos_mondrian_$(date +%Y%m%d_%H%M%S)"
          echo "📦 Using clone name: $CLONE_NAME"
          
          # Создаем новый клон
          echo "🔄 Creating new clone..."
          if crave clone create "$CLONE_NAME"; then
            echo "✅ Clone created successfully"
          else
            echo "❌ Failed to create clone"
            crave clone list || true
            exit 1
          fi
          
          # Получаем директорию клона
          cd "$(crave clone dir "$CLONE_NAME")"
          echo "📍 Working directory: $(pwd)"
          
          # Инициализируем repo
          echo "📥 Initializing repo..."
          repo init -u https://github.com/LF52406/android_manifest.git -b sixteen-qpr2 --git-lfs
          
          # Создаем локальные манифесты
          echo "📝 Creating local manifests..."
          rm -rf .repo/local_manifests
          mkdir -p .repo/local_manifests
          
          cat << 'EOF' > .repo/local_manifests/mondrian.xml
          <?xml version="1.0" encoding="UTF-8"?>
          <manifest>
              <project path="device/xiaomi/mondrian" name="LF52406/android_device_xiaomi_mondrian" remote="github" revision="lineage-23.2" />
              <project path="device/xiaomi/sm8450-common" name="LF52406/android_device_xiaomi_sm8450-common" remote="github" revision="lineage-23.2" />
              <project path="vendor/xiaomi/mondrian" name="LF52406/proprietary_vendor_xiaomi_mondrian" remote="github" revision="lineage-23.2" />
              <project path="vendor/xiaomi/sm8450-common" name="LF52406/proprietary_vendor_xiaomi_sm8450-common" remote="github" revision="lineage-23.2" />
              <project path="kernel/xiaomi/sm8450" name="LF52406/android_kernel_xiaomi_sm8450" remote="github" revision="lineage-23.2" />
              <project path="kernel/xiaomi/sm8450-modules" name="LineageOS/android_kernel_xiaomi_sm8450-modules" remote="github" revision="lineage-23.2" />
              <project path="kernel/xiaomi/sm8450-devicetrees" name="LineageOS/android_kernel_xiaomi_sm8450-devicetrees" remote="github" revision="lineage-23.2" />
              <project path="hardware/xiaomi" name="LineageOS/android_hardware_xiaomi" remote="github" revision="lineage-23.2" />
          </manifest>
          EOF
          
          echo "✅ Local manifest created:"
          cat .repo/local_manifests/mondrian.xml
          
          # Синхронизация
          echo "🔄 Starting repo sync (this will take 20-40 minutes)..."
          echo "⏱️ Started at: $(date)"
          
          repo sync -c -j8 --force-sync --no-tags --no-clone-bundle --fail-fast
          
          if [ $? -eq 0 ]; then
            echo "✅ Sync completed at $(date)"
          else
            echo "❌ Sync failed"
            exit 1
          fi

      - name: Run build
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          
          # Находим последний созданный клон
          CLONE_NAME=$(crave clone list | grep "pixelos" | tail -1 | awk '{print $1}')
          if [ -z "$CLONE_NAME" ]; then
            echo "❌ No pixelos clone found"
            exit 1
          fi
          
          cd "$(crave clone dir "$CLONE_NAME")"
          echo "📍 Build directory: $(pwd)"
          
          echo "🏗️ Starting build at $(date)"
          echo "⚙️ Build command: source build/envsetup.sh && lunch pixelos_mondrian-userdebug && mka bacon -j\$(nproc --all)"
          
          # Запускаем сборку
          crave run --clean --no-patch -- "source build/envsetup.sh && lunch pixelos_mondrian-userdebug && mka bacon -j\$(nproc --all)"
          
          if [ $? -eq 0 ]; then
            echo "✅ Build completed successfully at $(date)"
          else
            echo "❌ Build failed"
            exit 1
          fi

      - name: Download result
        if: always()
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          
          # Находим последний созданный клон
          CLONE_NAME=$(crave clone list | grep "pixelos" | tail -1 | awk '{print $1}')
          if [ -z "$CLONE_NAME" ]; then
            echo "❌ No pixelos clone found"
            exit 1
          fi
          
          cd "$(crave clone dir "$CLONE_NAME")"
          
          echo "📥 Checking for build artifacts..."
          
          # Создаем директорию для артефактов
          mkdir -p $HOME/artifacts
          cd $HOME/artifacts
          
          # Проверяем наличие zip файлов
          if crave ls out/target/product/mondrian/*.zip 2>/dev/null | grep -q ".zip"; then
            echo "✅ Found zip files, downloading..."
            crave pull out/target/product/mondrian/*.zip ./
            echo "✅ Downloaded files:"
            ls -la *.zip
          else
            echo "⚠️ No zip files found"
            echo "📁 Contents of out/target/product/mondrian/:"
            crave ls out/target/product/mondrian/ || echo "Directory not found"
          fi

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: PixelOS-mondrian-${{ github.run_number }}
          path: $HOME/artifacts/*.zip
          if-no-files-found: ignore
          retention-days: 30

      - name: Cleanup
        if: always()
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          echo "🧹 Cleaning up..."
          
          # Показываем список клонов
          echo "📋 Current clones:"
          crave clone list
          
          # Опционально: удаляем старые клоны (раскомментируйте если нужно)
          # crave clone list | grep "pixelos" | head -n -2 | while read -r line; do
          #   OLD_CLONE=$(echo "$line" | awk '{print $1}')
          #   echo "Deleting old clone: $OLD_CLONE"
          #   crave clone delete "$OLD_CLONE" || true
          # done
          
          echo "✅ Cleanup completed"
